const PORT = 3000;

const STATUS_OK = 200;
const STATUS_NOT_FOUND = 404;

const FILE_NAME = "uni_course_sphere_homepage.html";

const ERR_MSG_FILE_NF = "Error: File Not Found";

module.exports = {
  PORT,
  STATUS_OK,
  STATUS_NOT_FOUND,
  FILE_NAME,
  ERR_MSG_FILE_NF,
};
